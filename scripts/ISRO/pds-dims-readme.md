How to use the installer
Save the installer:
nano install-pds-dims.sh (paste the code above and save).

Run the installer:

Bash
bash install-pds-dims.sh
Activate it:

Bash
source ~/.bashrc